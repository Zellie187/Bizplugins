includes:
    - vendor/szepeviktor/phpstan-wordpress/extension.neon

parameters:

    level: 6

    paths:
        - includes

    tmpDir: .phpstan

    inferPrivatePropertyTypeFromConstructor: true

    ignoreErrors:
        # BIZUPKEEP_PAYMENTS_* path constants are define()'d at runtime by
        # bizupkeep-payments.php / Constants::register(); no stub package
        # can know about them ahead of time.
        - '#^Constant BIZUPKEEP_PAYMENTS_[A-Z_]+ not found\.$#'
